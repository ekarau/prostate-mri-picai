# Prostate MRI / PI-CAI evaluation — Python dependencies
# Tested on Google Colab (Python 3.12). Install with:
#   pip install -r requirements.txt
#
# NOTE: numpy must stay < 2. MONAI 1.3.2 still calls ndarray.ptp(),
# which NumPy 2.0 removed, so the transforms crash otherwise.

numpy==1.26.4
monai==1.3.2

# Medical image I/O
SimpleITK
nibabel
pydicom

# PI-CAI evaluation toolkit
picai_eval
report_guided_annotation

# Core scientific stack
scipy
scikit-learn
pandas

# Plotting
matplotlib
seaborn

# Misc
requests
